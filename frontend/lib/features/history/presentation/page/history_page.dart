import 'package:flutter/material.dart';

/// 履歴ページ (開発中)
class HistoryPage extends StatelessWidget {
  /// HistoryPageのコンストラクタ
  const HistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('履歴 (開発中)')));
  }
}
