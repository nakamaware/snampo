import 'package:flutter/material.dart';

/// プロフィールページ (開発中)
class ProfilePage extends StatelessWidget {
  /// ProfilePageのコンストラクタ
  const ProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('プロフィール (開発中)')));
  }
}
