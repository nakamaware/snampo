import 'package:flutter/material.dart';

/// マップページ (開発中)
class MapPage extends StatelessWidget {
  /// MapPageのコンストラクタ
  const MapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('マップ (開発中)')));
  }
}
