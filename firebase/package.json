{
  "name": "snampo-firebase-rules",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --project demo-snampo --only firestore,storage \"vitest run\"",
    "test:only": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "@types/node": "^26.6.2",
    "firebase": "^12.19.0",
    "firebase-tools": "^15.30.2",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
